use crate::{BrpClient, Result};

pub async fn upload(
    _client: &BrpClient,
    _filename: &str,
    _data_base64: &str,
    _subdir: Option<&str>,
) -> Result<()> {
    todo!("Implementation in Task 3")
}
