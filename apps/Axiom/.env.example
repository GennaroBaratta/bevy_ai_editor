# .env.example
# PostgreSQL Database Configuration
DB_NAME=mydatabase
DB_USER=user
DB_PASSWORD=password

# Rust Actix-web Backend Configuration
# RUST_LOG controls logging level, e.g., "info", "debug", "trace"
RUST_LOG=info
# The DATABASE_URL is constructed automatically in docker-compose.yml,
# but can be set here for local development outside Docker.
# DATABASE_URL=postgres://user:password@localhost:5432/mydatabase

# Vue 3 + Vite Frontend Configuration
# These are used by Vite when building the frontend.
# In docker-compose, these point to internal service names.
# For local dev, they might point to localhost.
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws